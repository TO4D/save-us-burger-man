# 커밋 메시지 작성 규칙

이 프로젝트의 커밋 메시지는 아래 형식을 사용합니다.

## 기본 형식

```text
[접두사] 한글 요약 메시지
```

예시:

```text
[추가] 주문 완료 화면 애니메이션 구현
[수정] 패티 굽기 타이머 오차 보정
[제거] 사용하지 않는 스토리보드 이미지 삭제
[리팩터링] 주문 매니저 상태 전환 로직 정리
[설정] 입력 시스템 패키지 버전 조정
[문서] 플레이 테스트 체크리스트 추가
```

## 작성 규칙

- 접두사는 대괄호(`[]`)로 감싸고 문장 맨 앞에 작성합니다.
- 접두사 뒤에는 한 칸을 띄우고 변경 내용을 한글로 요약합니다.
- 요약 메시지는 한 줄로 간결하게 작성합니다.
- 메시지 끝에 마침표는 붙이지 않습니다.
- 하나의 커밋은 하나의 목적만 담는 것을 원칙으로 합니다.

## 커밋 분할 원칙

- 커밋은 작업 순서보다 변경의 성격을 기준으로 분리합니다.
- 서로 다른 성격의 변경은 가능한 한 별도 커밋으로 나눕니다.
- 기능 추가와 버그 수정이 함께 있으면 분리합니다.
- 로직 변경과 리소스 교체가 함께 있으면 분리합니다.
- 리팩터링과 동작 변경이 함께 있으면 분리합니다.
- 설정 변경, 문서 수정, 테스트 보강은 기능 개발 커밋에 섞지 않는 것을 권장합니다.
- 하나의 커밋을 되돌렸을 때 의미가 명확해야 합니다.
- 리뷰어가 커밋 단위만 봐도 의도를 이해할 수 있어야 합니다.

## 분할 기준 예시

- 새로운 UI를 추가하면서 관련 프리팹도 만든 경우
  기능 구현 커밋과 프리팹/리소스 반영 커밋으로 나눌 수 있습니다.
- 버그를 수정하는 과정에서 변수명 정리까지 함께 한 경우
  버그 수정 커밋과 리팩터링 커밋으로 나눕니다.
- 기능 구현 후 테스트 코드와 문서를 보강한 경우
  구현 커밋, 테스트 커밋, 문서 커밋으로 나눕니다.

## 사용 가능한 접두사

- `[추가]`: 새로운 기능, 에셋, 화면, 파일 추가
- `[수정]`: 기존 기능 동작 수정, 버그 수정, 밸런스 조정
- `[제거]`: 불필요한 코드, 에셋, 설정 제거
- `[리팩터링]`: 동작 변화 없이 구조 개선
- `[설정]`: 프로젝트 설정, 패키지, 빌드 설정 변경
- `[문서]`: README, 가이드, 주석 등 문서성 변경
- `[테스트]`: 테스트 코드 또는 테스트 환경 변경

## 권장 예시

- `[추가] 조리대 버거 적층 크기 조절 옵션 추가`
- `[수정] 주문 카드 드래그 영역 판정 보정`
- `[제거] 미사용 손님 스프라이트 삭제`
- `[리팩터링] 주문 생성 로직 메서드 분리`
- `[설정] 렌더 텍스처 에셋 경로 정리`
- `[문서] 커밋 분할 기준 문서화`

## 피해야 할 예시

- `버그 수정`
- `[fix] bug fix`
- `[수정] fix bug`
- `[추가] 이것저것 수정`
- `[수정] UI 수정 및 사운드 교체 및 문서 업데이트`

## 선택 사항

필요하면 본문을 아래처럼 추가할 수 있습니다.

```text
[수정] 음료 생성 위치 스폰 보정

일부 해상도에서 음료가 조리대 밖에 생성되던 문제를 수정
SpawnPoint 계산 기준을 카메라 뷰포트 기준으로 변경
```
